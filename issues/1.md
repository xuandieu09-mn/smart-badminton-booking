## 🎯 Mục tiêu
Khởi tạo backend NestJS với cấu trúc module chuẩn và tích hợp Prisma Client.

## ✅ Checklist

### 1. Khởi tạo NestJS Project
```bash
# Đảm bảo đã cài NestJS CLI
npm install -g @nestjs/cli

# Tạo thư mục backend
mkdir backend
cd backend

# Khởi tạo NestJS project
nest new . --skip-git
# Chọn npm khi được hỏi package manager

# Cài dependencies cần thiết
npm install @prisma/client
npm install --save-dev prisma
npm install @nestjs/config class-validator class-transformer
npm install bcrypt
npm install --save-dev @types/bcrypt
```

### 2. Tạo Prisma Module

**File: `src/prisma/prisma.service.ts`**
```typescript
import { Injectable, OnModuleInit, OnModuleDestroy } from '@nestjs/common';
import { PrismaClient } from '@prisma/client';

@Injectable()
export class PrismaService extends PrismaClient implements OnModuleInit, OnModuleDestroy {
  async onModuleInit() {
    await this.$connect();
    console.log('✅ Prisma connected to database');
  }

  async onModuleDestroy() {
    await this.$disconnect();
    console.log('👋 Prisma disconnected from database');
  }
}
```

**File: `src/prisma/prisma.module.ts`**
```typescript
import { Global, Module } from '@nestjs/common';
import { PrismaService } from './prisma.service';

@Global()
@Module({
  providers: [PrismaService],
  exports: [PrismaService],
})
export class PrismaModule {}
```

### 3. Cấu trúc Module

Tạo các thư mục:
```bash
mkdir -p src/modules/{auth,users,courts,bookings,payments,wallet}
mkdir -p src/common/{decorators,guards,filters,interceptors}
```

**File: `src/app.module.ts`**
```typescript
import { Module } from '@nestjs/common';
import { ConfigModule } from '@nestjs/config';
import { PrismaModule } from './prisma/prisma.module';

@Module({
  imports: [
    ConfigModule.forRoot({
      isGlobal: true,
    }),
    PrismaModule,
    // Sẽ thêm các modules khác sau
  ],
})
export class AppModule {}
```

### 4. Copy Prisma Schema từ root project

```bash
# Trong thư mục backend
cp ../prisma/schema.prisma ./prisma/schema.prisma

# Generate Prisma Client
npx prisma generate
```

### 5. Cập nhật DATABASE_URL

**File: `backend/.env`**
```env
DATABASE_URL="postgresql://admin:adminpassword@localhost:5433/badminton_booking?schema=public"
PORT=3000
```

### 6. Test kết nối

**File: `src/main.ts`**
```typescript
import { NestFactory } from '@nestjs/core';
import { ValidationPipe } from '@nestjs/common';
import { AppModule } from './app.module';

async function bootstrap() {
  const app = await NestFactory.create(AppModule);
  
  app.useGlobalPipes(new ValidationPipe({
    whitelist: true,
    forbidNonWhitelisted: true,
    transform: true,
  }));
  
  app.enableCors();
  
  await app.listen(3000);
  console.log('🚀 Backend running on http://localhost:3000');
}
bootstrap();
```

Chạy thử:
```bash
npm run start:dev
```

## 🎯 Acceptance Criteria
- [ ] NestJS server chạy thành công trên port 3000
- [ ] Prisma Service connect được tới database
- [ ] Console log hiện "✅ Prisma connected to database"
- [ ] Folder structure chuẩn theo modules

## 📚 References
- [NestJS Documentation](https://docs.nestjs.com)
- [Prisma with NestJS](https://docs.nestjs.com/recipes/prisma)